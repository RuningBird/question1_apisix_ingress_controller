apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "xcoding.fullname" . }}-test-connection"
  labels:
    {{- include "xcoding.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "xcoding.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
